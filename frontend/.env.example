# Public, build-time URL of a separately hosted SecureLink API used by Vite.
# This value is embedded in the frontend bundle and must not contain a secret.
# Omit it for Vercel's same-origin production deployment.
VITE_API_BASE=http://localhost:8000
